# Database Configuration
# The Go code specifically looks for DATABASE_URL
DATABASE_URL=postgres://postgres:123456789@localhost:5432/postgres

# Email (SMTP) Configuration
# The Go code looks for SMTP_EMAIL, SMTP_PASS, SMTP_HOST, and SMTP_PORT
SMTP_EMAIL=devillakelvinjohn@gmail.com
SMTP_PASS=abcdefghijklmnop
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587

# Frontend Configuration
FRONTEND_URL=http://localhost:3000

# Server Configuration
PORT=8080
GIN_MODE=debug