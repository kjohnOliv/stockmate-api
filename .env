# Database Configuration
DB_USER=postgres
DB_PASS=123456789
DB_HOST=localhost
DB_PORT=5432
DB_NAME=postgres
# This is the full URL constructed from the parts above
DATABASE_URL=postgresql://${DB_USER}:${DB_PASS}@${DB_HOST}:${DB_PORT}/${DB_NAME}

# Email (SMTP) Configuration
# Use an "App Password" from Google, NOT your regular login password
SMTP_USER=devillakelvinjohn@gmail.com
SMTP_PASS=zxcvbnmqwertuiop
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587

# Frontend URL (For generating reset links)
FRONTEND_URL=http://localhost:3000